dynamicOnLoad(function() {
	var input = {
		propertyName : "template-element",
		roleName : "role-label",
		eventName : "onclick",
		handlerName : "click"
	};
	dynamicHandlerAttach(input.propertyName, input.roleName, input.eventName, input.handlerName);
});

dynamicOnLoad(function() {
	var input = {
		propertyName : "template-element",
		roleName : "role-label",
		eventName : "ondblclick",
		handlerName : "dblClick"
	};
	dynamicHandlerAttach(input.propertyName, input.roleName, input.eventName, input.handlerName);
});

dynamicOnLoad(function() {
	var input = {
		propertyName : "template-element",
		roleName : "role-label",
		eventName : "onmousedown",
		handlerName : "mouseDown"
	};
	dynamicHandlerAttach(input.propertyName, input.roleName, input.eventName, input.handlerName);
});

dynamicOnLoad(function() {
	var input = {
		propertyName : "template-element",
		roleName : "role-label",
		eventName : "onmouseup",
		handlerName : "mouseUp"
	};
	dynamicHandlerAttach(input.propertyName, input.roleName, input.eventName, input.handlerName);
});

dynamicOnLoad(function() {
	var input = {
		propertyName : "template-element",
		roleName : "role-label",
		eventName : "onmouseover",
		handlerName : "mouseOver"
	};
	dynamicHandlerAttach(input.propertyName, input.roleName, input.eventName, input.handlerName);
});

dynamicOnLoad(function() {
	var input = {
		propertyName : "template-element",
		roleName : "role-label",
		eventName : "onmouseout",
		handlerName : "mouseOut"
	};
	dynamicHandlerAttach(input.propertyName, input.roleName, input.eventName, input.handlerName);
});

dynamicOnLoad(function() {
	var input = {
		propertyName : "template-element",
		roleName : "role-label",
		eventName : "onmousemove",
		handlerName : "mouseMove"
	};
	dynamicHandlerAttach(input.propertyName, input.roleName, input.eventName, input.handlerName);
});

